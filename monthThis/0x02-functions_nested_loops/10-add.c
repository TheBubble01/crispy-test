#include "main.h"
/**
 * add - adds two integers and return the result
 * @a: a value (integer) to be added to b
 * @b: an integer to be added with a
 * Return: the value of c
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
